# Exclui pacotes de dependência
/node_modules
/.pnp
.pnp.js

# Exclui a pasta de compilação do Next.js
/.next/
/out/

# Exclui ficheiros de cache
/npm-debug.log
yarn-error.log
/yarn-cache
/package-lock.json # Se você usa npm, mantenha este. Se usar pnpm, ignore
# Se você usa pnpm, mantenha /pnpm-lock.yaml e ignore este:
# /pnpm-lock.yaml

# Exclui variáveis de ambiente e segredos
# ESTE É OBRIGATÓRIO PARA SEGURANÇA!
.env
.env.local
.env.development
.env.production
.vercel # Ficheiro de configuração do Vercel local

# Exclui ficheiros gerados pelo TypeScript/IDE
*.tsbuildinfo
.DS_Store # macOS
Thumbs.db # Windows
.vscode # Configurações da IDE (opcional, mas recomendado)
```eof
